---
import { Image } from "astro:assets";
import AppStoreBadge from "./AppStoreBadge.astro";
import { SUBLINE, href } from "../consts";
import phoneShot from "../assets/framed/hero-phone.png";
import padShot from "../assets/framed/hero-pad.png";
---

<section class="hero section-alt">
  <div class="wrap grid">
    <div class="copy">
      <h1 class="display">
        The week's rota,<br /><span class="accent">built in a tap.</span>
      </h1>
      <p class="lede">{SUBLINE}</p>
      <div class="cta">
        <AppStoreBadge />
        <a class="btn btn-quiet" href={href("/support")}>Questions? Support</a>
      </div>
      <p class="platforms">For iPhone and iPad · syncs through your own iCloud</p>
    </div>
    <div class="art" aria-hidden="true">
      <div class="pad">
        <Image
          src={padShot}
          alt=""
          widths={[520, 780, 1100]}
          sizes="(max-width: 900px) 84vw, 560px"
          loading="eager"
        />
      </div>
      <div class="phone">
        <Image
          src={phoneShot}
          alt=""
          widths={[300, 460, 640]}
          sizes="(max-width: 900px) 40vw, 250px"
          loading="eager"
          fetchpriority="high"
        />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    overflow: hidden;
    padding-block: clamp(56px, 8vw, 104px);
  }

  .grid {
    display: grid;
    grid-template-columns: minmax(0, 5fr) minmax(0, 6fr);
    align-items: center;
    gap: clamp(2rem, 5vw, 4rem);
  }

  .copy {
    display: flex;
    flex-direction: column;
    gap: 1.6rem;
  }

  .cta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.9rem;
  }

  .platforms {
    font-size: 0.9rem;
    color: var(--faint);
  }

  /* Overlapped iPad-behind / iPhone-front composition. Tilt and device
     bodies are baked into the transparent framed PNGs — no CSS effects. */
  .art {
    position: relative;
    min-height: 340px;
  }

  .pad {
    margin-left: 12%;
  }

  .phone {
    position: absolute;
    width: 34%;
    bottom: -8%;
    left: -2%;
  }

  @media (max-width: 900px) {
    .grid {
      grid-template-columns: 1fr;
    }

    .art {
      max-width: 560px;
      margin-top: 1rem;
    }

    .phone {
      width: 32%;
      left: 0;
    }
  }
</style>
