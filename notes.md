TODO:
1. Availability tab should be per employee, not an option available in general, but only on the employee tab. Just delete the availability tab.
2. Place the employee list on the left underneath the main options on the top left. 
3. On the shifts tab, each weekday should be represented by a row, and the shifts should not be in a grid, but in a list.
    - Example: Monday | (7 - 2, 1 person), (8 - 3, 2 people), (16 - 23, 2 people)
4. On the availability calendar for each employee, 
    - I want the weekday to be represented by a column
    - Instead of clicking to cycle, I want a click to just select a cell, or multiple cells
        - there should be a widget for each availability option, which once clicked, will change the selected cell(s)


